# Steering microservice only (NOT needed by the base chatbot). Install into a separate venv:
#   python -m venv steering/steer-env && source steering/steer-env/bin/activate
#   pip install torch --index-url https://download.pytorch.org/whl/cu128   # <- match `nvidia-smi`
#   pip install -r steering/requirements-steer.txt
# Install torch FIRST, from the cu12x index matching the local driver's CUDA version, or CUDA init
# fails with "driver too old". cu128 suits a 12.8 driver; use cu124/cu126 on older ones.
# Needs Python <=3.12 (no torch/bitsandbytes wheels for 3.13+).
transformers>=4.51
accelerate>=0.30
bitsandbytes>=0.43
numpy
fastapi
uvicorn[standard]
